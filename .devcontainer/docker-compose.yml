name: recruitment-assistant

services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace/recruitment-assistant:cached
    working_dir: /workspace/recruitment-assistant
    command: sleep infinity
